/*
import { SlashCommandBuilder } from "@discordjs/builders";

export default {
    async execute(interaction) {
        interaction.reply("It is I!");
    },
    data: new SlashCommandBuilder()
        .setName('queen')
        .setDescription('Tells you who is the queen (hint: it is Satania!)')
        .addStringOption(option => {
            return option.setName('which')
                .setDescription('Allows you to specify a specific smuggy picture instead of a randomized one')
                .setRequired(false);
        })
};
*/